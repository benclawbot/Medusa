# Desktop recovery UX

The desktop client consumes the presentation-neutral recovery view emitted by the runtime. Recoverable sessions are shown in a modal operator workflow with checkpoint metadata, restore previews, integrity warnings, action availability, and explicit confirmation before destructive restore actions.

The runtime remains authoritative for preflight checks, action authorization, execution, and audit persistence.
