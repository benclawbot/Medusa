diff --git a/crates/medusa-config/src/lib.rs b/crates/medusa-config/src/lib.rs
index 6b97df9..1111111 100644
--- a/crates/medusa-config/src/lib.rs
+++ b/crates/medusa-config/src/lib.rs
@@ -175,7 +175,7 @@ impl Default for MemoryConfig {
         Self {
             enabled: true,
             format: "markdown".into(),
-            auto_promote_low_risk: true,
+            auto_promote_low_risk: false,
         }
     }
 }
@@ -249,6 +249,11 @@ impl Config {
         if self.memory.format != "markdown" {
             return Err(invalid("memory format must remain markdown"));
         }
+        if self.memory.auto_promote_low_risk {
+            return Err(invalid(
+                "memory.auto_promote_low_risk is unsupported; lessons must pass probation and be promoted through explicit, rollback-capable graduation",
+            ));
+        }
         Ok(())
     }
 }
@@ -431,6 +436,7 @@ mod tests {
         config.validate().expect("defaults");
         assert_eq!(config.model.context_window_tokens, 1_000_000);
         assert_eq!(config.model.auto_compact_percent, 40);
+        assert!(!config.memory.auto_promote_low_risk);
     }
@@ -472,6 +478,16 @@ mod tests {
     fn force_push_fails_closed() {
         assert!(Config::from_toml("version = 1\n[git]\nallow_force_push = true\n").is_err());
     }
+
+    #[test]
+    fn automatic_memory_promotion_fails_closed() {
+        let error = Config::from_toml(
+            "version = 1\n[memory]\nauto_promote_low_risk = true\n",
+        )
+        .expect_err("automatic promotion must be rejected");
+        assert!(error
+            .to_string()
+            .contains("explicit, rollback-capable graduation"));
+    }
 }
diff --git a/crates/medusa-agent/src/session/completed_learning.rs b/crates/medusa-agent/src/session/completed_learning.rs
index 3b1e309..2222222 100644
--- a/crates/medusa-agent/src/session/completed_learning.rs
+++ b/crates/medusa-agent/src/session/completed_learning.rs
@@ -65,7 +65,17 @@ fn admit_to_canonical_memory(
 
     proposal["lifecycle"] = json!({
         "status": status,
-        "auto_promotion": "disabled_until_probation_passes",
+        "auto_promotion": "disabled",
+        "promotion": {
+            "mode": "explicit_graduation",
+            "command": "medusa skills graduate NAME --confirm",
+            "requires_probation_state": "passed"
+        },
+        "rollback": {
+            "mode": "graduation_receipt_transaction",
+            "on_receipt_failure": "restore_previous_lifecycle_state"
+        },
         "minimum_confidence_milli": MIN_PROBATION_CONFIDENCE_MILLI,
         "rejection_reason": if status == "rejected" {
             "insufficient confidence, evidence, or safety"
@@ -177,6 +187,16 @@ mod tests {
         assert_eq!(value["provenance"]["verification_result"], "verified");
         assert_eq!(value["lifecycle"]["status"], "probation");
+        assert_eq!(value["lifecycle"]["auto_promotion"], "disabled");
+        assert_eq!(
+            value["lifecycle"]["promotion"]["mode"],
+            "explicit_graduation"
+        );
+        assert_eq!(
+            value["lifecycle"]["rollback"]["mode"],
+            "graduation_receipt_transaction"
+        );
         assert!(processed_marker(&session).is_file());
     }
