diff --git a/apps/medusa-desktop/src/App.tsx b/apps/medusa-desktop/src/App.tsx
--- a/apps/medusa-desktop/src/App.tsx
+++ b/apps/medusa-desktop/src/App.tsx
@@ -26,6 +26,7 @@ import React, { useCallback, useEffect, useMemo, useRef, useState } from "react";
 import { ApprovalCard } from "./ApprovalCard";
+import { VoiceControls } from "./VoiceControls";
 import "./approval-card.css";
@@ -767,6 +768,12 @@ export function App() {
                   <div className="composer-actions">
+                    <VoiceControls capability={{
+                      available: false,
+                      reason: "Realtime voice is unavailable because the configured ChatGPT OAuth gateway does not expose an authenticated Realtime route. Microphone capture has not started.",
+                    }} />
                     {busy && <button className="cancel-button" onClick={cancel}><Square size={13} /> Cancel</button>}
