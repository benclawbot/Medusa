diff --git a/crates/medusa-agent/src/session.rs b/crates/medusa-agent/src/session.rs
--- a/crates/medusa-agent/src/session.rs
+++ b/crates/medusa-agent/src/session.rs
@@ -192,7 +192,7 @@ pub fn bootstrap(repo: &Path) -> MedusaResult<()> {
             record_nonfatal(repo, None, stage, operation, &error.to_string());
         }
     }
-    let map = repo.join("REPOSITORY_MAP.md");
+    let map = repo.join(".medusa/REPOSITORY_MAP.md");
     if !map.exists() {
         if let Err(error) = fs::write(
             &map,
