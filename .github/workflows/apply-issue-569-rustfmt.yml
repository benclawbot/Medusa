name: Apply issue 569 rustfmt correction

on:
  push:
    branches:
      - agent/runtime-durable-controller-events
    paths:
      - .github/issue-569-rustfmt.patch
      - .github/workflows/apply-issue-569-rustfmt.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply rustfmt patch
        run: |
          set -euo pipefail
          git apply --check .github/issue-569-rustfmt.patch
          git apply .github/issue-569-rustfmt.patch
          rm .github/issue-569-rustfmt.patch
          rm .github/workflows/apply-issue-569-rustfmt.yml
      - name: Commit correction
        run: |
          set -euo pipefail
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "Format durable controller events"
          git push origin HEAD:${GITHUB_REF_NAME}
