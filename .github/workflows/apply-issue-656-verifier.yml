name: Apply issue 656 verifier fix

on:
  push:
    branches:
      - codex/656-truthful-code-intelligence
    paths:
      - .github/workflows/apply-issue-656-verifier.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/656-truthful-code-intelligence
          fetch-depth: 0
      - name: Apply validated verifier patch
        shell: bash
        run: |
          set -euo pipefail
          cat .github/patches/issue-656-verifier/part-* | base64 --decode > /tmp/issue-656-verifier.patch
          git apply --check /tmp/issue-656-verifier.patch
          git apply /tmp/issue-656-verifier.patch
          rm -rf .github/patches/issue-656-verifier
          rm .github/workflows/apply-issue-656-verifier.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Preserve repository Python verifier evidence"
          git push origin HEAD:codex/656-truthful-code-intelligence
