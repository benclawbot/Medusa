name: Update Windows Sandbox Lockfile

on:
  push:
    branches:
      - agent/issue-308-native-windows-appcontainer

permissions:
  contents: write

jobs:
  update-lock:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/issue-308-native-windows-appcontainer
      - uses: dtolnay/rust-toolchain@1.88.0
      - run: cargo generate-lockfile
      - name: Commit lockfile
        run: |
          rm .github/workflows/update-windows-sandbox-lock.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add Cargo.lock .github/workflows/update-windows-sandbox-lock.yml
          git commit -m "Update lockfile for Windows sandbox"
          git push origin HEAD:agent/issue-308-native-windows-appcontainer
