name: Apply frontend telemetry patch

on:
  push:
    branches:
      - roadmap/161-frontend-telemetry

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@1.88.0
        with:
          components: rustfmt
      - name: Apply authoritative telemetry frontend patch
        run: python3 scripts/apply-frontend-telemetry.py
      - name: Format workspace
        run: cargo fmt --all
      - name: Commit generated implementation
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "feat(usage): surface durable session telemetry" || exit 0
          git push
