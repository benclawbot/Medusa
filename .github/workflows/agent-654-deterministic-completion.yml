name: Agent 654 Deterministic Completion

on:
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - scripts/apply-agent-654-deterministic-completion.py
      - .github/workflows/agent-654-deterministic-completion.yml

permissions:
  contents: write

concurrency:
  group: agent-654-deterministic-completion
  cancel-in-progress: true

jobs:
  apply-validate-commit:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@39b0b3842c7e8bbf6904c0bfc3d9006fdd4dc4e0 # 1.88.0
        with:
          components: rustfmt,clippy
      - name: Apply deterministic completion patch
        run: python3 scripts/apply-agent-654-deterministic-completion.py
      - name: Format workspace
        run: cargo fmt --all
      - name: Validate runtime tests
        run: cargo test -p medusa-runtime --locked
      - name: Validate runtime lint
        run: cargo clippy -p medusa-runtime --all-targets --all-features --locked -- -D warnings
      - name: Validate architecture guards
        run: |
          python3 scripts/check-mutation-lifecycle.py
          python3 scripts/check-evidence-authority.py
      - name: Remove temporary patch machinery
        run: rm scripts/apply-agent-654-deterministic-completion.py .github/workflows/agent-654-deterministic-completion.yml
      - name: Commit validated implementation
        shell: bash
        run: |
          set -euo pipefail
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git diff --cached --check
          git commit -m "Complete accepted mutations without a redundant model turn"
          git push origin HEAD:${{ github.head_ref }}
