name: temp wakeup prepare
on:
  push:
    branches: [agent/event-driven-wakeups-main]
permissions:
  contents: write
jobs:
  prepare:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/event-driven-wakeups-main
      - uses: dtolnay/rust-toolchain@1.88.0
      - run: cargo fmt --all
      - run: cargo generate-lockfile
      - run: rm .github/workflows/temp-wakeup-prepare.yml
      - run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m 'chore(wakeup): format and refresh lockfile'
          git push origin HEAD:agent/event-driven-wakeups-main
