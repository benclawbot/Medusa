name: Apply Desktop Commander MCP integration

on:
  push:
    branches: [feat/desktop-commander-mcp]

permissions:
  contents: read

concurrency:
  group: apply-desktop-commander-mcp-${{ github.ref }}
  cancel-in-progress: false

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/desktop-commander-mcp
          fetch-depth: 0
      - name: Apply guarded integration patch
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path

          desktop = r'''use std::{
    collections::{BTreeMap, BTreeSet},
    env, fs,
    io::{self, BufRead, BufReader, Write},
    path::{Component, Path, PathBuf},
    process::{Child, ChildStdin, Command, Stdio},
    sync::mpsc::{self, Receiver, RecvTimeoutError},
    thread,
    time::{Duration, Instant},
};

use medusa_core::{ErrorCategory, ErrorCode, MedusaError, MedusaResult};
use serde_json::{Value, json};

use crate::{mcp::validate_mcp_output, redaction::redact_value};

const PINNED_PACKAGE: &str = "@wonderwhy-er/desktop-commander@0.2.46";
const DEFAULT_READ_TOOLS: &[&str] = &[
    "read_file",
    "read_multiple_files",
    "list_directory",
    "get_file_info",
    "start_search",
    "get_more_search_results",
    "stop_search",
    "list_searches",
];
const WRITE_TOOLS: &[&str] = &[
    "write_file",
    "create_directory",
    "move_file",
    "edit_block",
    "write_pdf",
];
const PROCESS_TOOLS: &[&str] = &[
    "start_process",
    "read_process_output",
    "interact_with_process",
    "force_terminate",
    "list_sessions",
    "kill_process",
    "list_processes",
];
const FORBIDDEN_META_TOOLS: &[&str] = &[
    "get_config",
    "set_config_value",
    "get_usage_stats",
    "give_feedback_to_desktop_commander",
    "get_prompts",
    "get_recent_tool_calls",
];

#[derive(Clone, Debug, Eq, PartialEq)]
pub struct DesktopCommanderSettings {
    enabled: bool,
    command: PathBuf,
    args: Vec<String>,
    allowed_tools: BTreeSet<String>,
    allow_write: bool,
    allow_process: bool,
    timeout: Duration,
    max_output_bytes: usize,
    configuration_error: Option<String>,
}

impl Default for DesktopCommanderSettings {
    fn default() -> Self {
        Self {
            enabled: false,
            command: PathBuf::from(if cfg!(windows) { "npx.cmd" } else { "npx" }),
            args: vec![
                "-y".to_owned(),
                PINNED_PACKAGE.to_owned(),
                "--no-onboarding".to_owned(),
            ],
            allowed_tools: DEFAULT_READ_TOOLS.iter().map(|tool| (*tool).to_owned()).collect(),
            allow_write: false,
            allow_process: false,
            timeout: Duration::from_secs(30),
            max_output_bytes: 256 * 1024,
            configuration_error: None,
        }
    }
}

impl DesktopCommanderSettings {
    #[must_use]
    pub fn from_env() -> Self {
        let mut settings = Self::default();
        settings.enabled = env_flag("MEDUSA_DESKTOP_COMMANDER_ENABLED");
        if let Ok(command) = env::var("MEDUSA_DESKTOP_COMMANDER_COMMAND") {
            if !command.trim().is_empty() {
                settings.command = PathBuf::from(command);
            }
        }
        if let Ok(raw) = env::var("MEDUSA_DESKTOP_COMMANDER_ARGS") {
            match serde_json::from_str::<Vec<String>>(&raw) {
                Ok(args) if !args.is_empty() => settings.args = args,
                Ok(_) => settings.configuration_error = Some(
                    "MEDUSA_DESKTOP_COMMANDER_ARGS must contain at least one argument".to_owned(),
                ),
                Err(error) => settings.configuration_error = Some(format!(
                    "MEDUSA_DESKTOP_COMMANDER_ARGS must be a JSON string array: {error}"
                )),
            }
        }
        if let Ok(raw) = env::var("MEDUSA_DESKTOP_COMMANDER_ALLOWED_TOOLS") {
            let allowed = csv_set(&raw);
            if !allowed.is_empty() {
                settings.allowed_tools = allowed;
            }
        }
        settings.allow_write = env_flag("MEDUSA_DESKTOP_COMMANDER_ALLOW_WRITE");
        settings.allow_process = env_flag("MEDUSA_DESKTOP_COMMANDER_ALLOW_PROCESS");
        settings.timeout = Duration::from_millis(env_u64(
            "MEDUSA_DESKTOP_COMMANDER_TIMEOUT_MS",
            30_000,
        ));
        settings.max_output_bytes = env_usize(
            "MEDUSA_DESKTOP_COMMANDER_MAX_OUTPUT_BYTES",
            256 * 1024,
        )
        .max(1024);
        settings
    }

    #[must_use]
    pub fn requested(&self) -> bool {
        self.enabled
    }

    #[must_use]
    pub fn enabled(&self) -> bool {
        self.enabled && self.configuration_error.is_none()
    }

    #[must_use]
    pub fn command(&self) -> &Path {
        &self.command
    }

    #[must_use]
    pub fn args(&self) -> &[String] {
        &self.args
    }

    #[must_use]
    pub fn configuration_error(&self) -> Option<&str> {
        self.configuration_error.as_deref()
    }

    #[must_use]
    pub fn package_label(&self) -> String {
        self.args
            .iter()
            .find(|arg| arg.contains("desktop-commander@"))
            .cloned()
            .unwrap_or_else(|| self.command.display().to_string())
    }

    #[must_use]
    pub fn effective_tools(&self) -> BTreeSet<String> {
        self.allowed_tools
            .iter()
            .filter(|tool| self.tool_allowed(tool, false))
            .cloned()
            .collect()
    }

    fn tool_allowed(&self, tool: &str, read_only: bool) -> bool {
        if FORBIDDEN_META_TOOLS.contains(&tool) || !self.allowed_tools.contains(tool) {
            return false;
        }
        if desktop_commander_tool_is_mutating(tool) && (!self.allow_write || read_only) {
            return false;
        }
        if is_process_tool(tool) && (!self.allow_process || read_only) {
            return false;
        }
        true
    }
}

pub struct DesktopCommanderClient {
    child: Child,
    stdin: ChildStdin,
    messages: Receiver<Result<Value, String>>,
    next_id: u64,
    discovered_tools: BTreeSet<String>,
    settings: DesktopCommanderSettings,
}

impl DesktopCommanderClient {
    pub fn connect(repo: &Path, settings: DesktopCommanderSettings) -> MedusaResult<Self> {
        if !settings.enabled() {
            return Err(invalid(
                settings
                    .configuration_error()
                    .unwrap_or("Desktop Commander MCP is not enabled"),
            ));
        }
        let repo = repo.canonicalize()?;
        let state = repo.join(".medusa/extensions/desktop-commander");
        let home = state.join("home");
        prepare_profile(&repo, &home)?;

        let mut command = Command::new(settings.command());
        command
            .args(settings.args())
            .current_dir(&repo)
            .env_clear()
            .stdin(Stdio::piped())
            .stdout(Stdio::piped())
            .stderr(Stdio::piped());
        copy_safe_environment(&mut command);
        let app_data = home.join("appdata");
        let cache = state.join("npm-cache");
        fs::create_dir_all(&app_data)?;
        fs::create_dir_all(&cache)?;
        command
            .env("HOME", &home)
            .env("USERPROFILE", &home)
            .env("APPDATA", &app_data)
            .env("LOCALAPPDATA", &app_data)
            .env("npm_config_cache", &cache)
            .env("NO_COLOR", "1")
            .env("CI", "1");

        let mut child = command.spawn().map_err(|error| {
            execution(format!(
                "start Desktop Commander with {}: {error}",
                settings.command().display()
            ))
        })?;
        let stdin = child
            .stdin
            .take()
            .ok_or_else(|| execution("Desktop Commander stdin unavailable"))?;
        let stdout = child
            .stdout
            .take()
            .ok_or_else(|| execution("Desktop Commander stdout unavailable"))?;
        if let Some(mut stderr) = child.stderr.take() {
            thread::spawn(move || {
                let _ = io::copy(&mut stderr, &mut io::sink());
            });
        }
        let messages = spawn_reader(stdout, settings.max_output_bytes);
        let mut client = Self {
            child,
            stdin,
            messages,
            next_id: 1,
            discovered_tools: BTreeSet::new(),
            settings,
        };
        client.initialize()?;
        Ok(client)
    }

    pub fn call_tool(
        &mut self,
        repo: &Path,
        tool: &str,
        arguments: &Value,
        read_only: bool,
    ) -> MedusaResult<Value> {
        if !self.discovered_tools.contains(tool) {
            return Err(invalid(format!(
                "Desktop Commander did not advertise tool {tool}"
            )));
        }
        if !self.settings.tool_allowed(tool, read_only) {
            return Err(policy(format!(
                "Desktop Commander tool {tool} is outside Medusa's configured capability policy"
            )));
        }
        if is_process_tool(tool) {
            validate_process_request(tool, arguments)?;
        }
        let arguments = sanitize_arguments(repo, arguments)?;
        let mut result = self.request(
            "tools/call",
            json!({"name": tool, "arguments": arguments}),
        )?;
        redact_value(&mut result);
        validate_mcp_output(&result)?;
        Ok(result)
    }

    fn initialize(&mut self) -> MedusaResult<()> {
        self.request(
            "initialize",
            json!({
                "protocolVersion": "2025-06-18",
                "capabilities": {},
                "clientInfo": {"name": "medusa", "version": env!("CARGO_PKG_VERSION")}
            }),
        )?;
        self.send(json!({
            "jsonrpc": "2.0",
            "method": "notifications/initialized"
        }))?;
        let result = self.request("tools/list", json!({}))?;
        let tools = result
            .get("tools")
            .and_then(Value::as_array)
            .ok_or_else(|| execution("Desktop Commander tools/list returned no tools array"))?;
        self.discovered_tools = tools
            .iter()
            .filter_map(|tool| tool.get("name").and_then(Value::as_str))
            .map(str::to_owned)
            .collect();
        if self.discovered_tools.is_empty() {
            return Err(execution("Desktop Commander advertised no MCP tools"));
        }
        Ok(())
    }

    fn request(&mut self, method: &str, params: Value) -> MedusaResult<Value> {
        let id = self.next_id;
        self.next_id = self.next_id.saturating_add(1);
        self.send(json!({
            "jsonrpc": "2.0",
            "id": id,
            "method": method,
            "params": params
        }))?;
        let deadline = Instant::now() + self.settings.timeout;
        loop {
            let remaining = deadline.saturating_duration_since(Instant::now());
            if remaining.is_zero() {
                return Err(execution(format!(
                    "Desktop Commander timed out waiting for {method}"
                )));
            }
            let message = match self.messages.recv_timeout(remaining) {
                Ok(Ok(message)) => message,
                Ok(Err(error)) => return Err(execution(error)),
                Err(RecvTimeoutError::Timeout) => {
                    return Err(execution(format!(
                        "Desktop Commander timed out waiting for {method}"
                    )));
                }
                Err(RecvTimeoutError::Disconnected) => {
                    return Err(execution("Desktop Commander output stream closed"));
                }
            };
            if message.get("id").and_then(Value::as_u64) != Some(id) {
                continue;
            }
            if let Some(error) = message.get("error") {
                return Err(execution(format!(
                    "Desktop Commander {method} failed: {error}"
                )));
            }
            return message
                .get("result")
                .cloned()
                .ok_or_else(|| execution(format!("Desktop Commander {method} returned no result")));
        }
    }

    fn send(&mut self, value: Value) -> MedusaResult<()> {
        serde_json::to_writer(&mut self.stdin, &value)?;
        self.stdin.write_all(b"\n")?;
        self.stdin.flush()?;
        Ok(())
    }
}

impl Drop for DesktopCommanderClient {
    fn drop(&mut self) {
        let _ = self.child.kill();
        let _ = self.child.wait();
    }
}

#[must_use]
pub fn desktop_commander_tool_is_mutating(tool: &str) -> bool {
    WRITE_TOOLS.contains(&tool)
}

fn is_process_tool(tool: &str) -> bool {
    PROCESS_TOOLS.contains(&tool)
}

fn spawn_reader(
    stdout: impl io::Read + Send + 'static,
    max_output_bytes: usize,
) -> Receiver<Result<Value, String>> {
    let (sender, receiver) = mpsc::channel();
    thread::spawn(move || {
        let mut reader = BufReader::new(stdout);
        loop {
            let mut buffer = Vec::new();
            match reader.read_until(b'\n', &mut buffer) {
                Ok(0) => break,
                Ok(_) if buffer.len() > max_output_bytes => {
                    let _ = sender.send(Err(format!(
                        "Desktop Commander response exceeded {max_output_bytes} bytes"
                    )));
                    break;
                }
                Ok(_) => {
                    while matches!(buffer.last(), Some(b'\n' | b'\r')) {
                        buffer.pop();
                    }
                    if buffer.is_empty() {
                        continue;
                    }
                    match serde_json::from_slice(&buffer) {
                        Ok(value) => {
                            if sender.send(Ok(value)).is_err() {
                                break;
                            }
                        }
                        Err(error) => {
                            let _ = sender.send(Err(format!(
                                "Desktop Commander emitted invalid JSON: {error}"
                            )));
                            break;
                        }
                    }
                }
                Err(error) => {
                    let _ = sender.send(Err(format!(
                        "read Desktop Commander output: {error}"
                    )));
                    break;
                }
            }
        }
    });
    receiver
}

fn prepare_profile(repo: &Path, home: &Path) -> MedusaResult<()> {
    let config_dir = home.join(".claude-server-commander");
    fs::create_dir_all(&config_dir)?;
    let config = json!({
        "blockedCommands": [
            "rm", "sudo", "shutdown", "reboot", "mkfs", "format", "mount", "umount",
            "dd", "fdisk", "parted", "diskpart", "su", "passwd", "useradd", "usermod",
            "iptables", "netsh", "reg", "sc", "runas", "cipher", "takeown"
        ],
        "allowedDirectories": [repo.display().to_string()],
        "telemetryEnabled": false,
        "pendingWelcomeOnboarding": false,
        "welcomeOnboardingEligible": false,
        "fileReadLineLimit": 1000,
        "fileWriteLineLimit": 200
    });
    fs::write(
        config_dir.join("config.json"),
        serde_json::to_vec_pretty(&config)?,
    )?;
    Ok(())
}

fn copy_safe_environment(command: &mut Command) {
    for key in [
        "PATH", "Path", "PATHEXT", "SystemRoot", "ComSpec", "WINDIR", "TEMP", "TMP",
        "TMPDIR", "SHELL",
    ] {
        if let Some(value) = env::var_os(key) {
            command.env(key, value);
        }
    }
}

fn sanitize_arguments(repo: &Path, arguments: &Value) -> MedusaResult<Value> {
    if !arguments.is_object() {
        return Err(invalid("Desktop Commander arguments must be a JSON object"));
    }
    let root = repo.canonicalize()?;
    let mut safe = arguments.clone();
    rewrite_paths(&root, &mut safe, None)?;
    Ok(safe)
}

fn rewrite_paths(root: &Path, value: &mut Value, key: Option<&str>) -> MedusaResult<()> {
    match value {
        Value::String(raw) if key.is_some_and(is_path_key) => {
            *raw = secure_path(root, raw)?.display().to_string();
        }
        Value::Array(values) if key.is_some_and(is_path_key) => {
            for value in values {
                let raw = value
                    .as_str()
                    .ok_or_else(|| invalid("every Desktop Commander path must be a string"))?;
                *value = Value::String(secure_path(root, raw)?.display().to_string());
            }
        }
        Value::Array(values) => {
            for value in values {
                rewrite_paths(root, value, key)?;
            }
        }
        Value::Object(values) => {
            for (key, value) in values {
                rewrite_paths(root, value, Some(key))?;
            }
        }
        _ => {}
    }
    Ok(())
}

fn is_path_key(key: &str) -> bool {
    let key = key.to_ascii_lowercase();
    key.contains("path") || matches!(key.as_str(), "source" | "destination" | "directory")
}

fn secure_path(root: &Path, raw: &str) -> MedusaResult<PathBuf> {
    if raw.trim().is_empty() || raw.starts_with('~') || raw.contains('\0') {
        return Err(policy("Desktop Commander path is empty, contains NUL, or uses ~ expansion"));
    }
    let path = Path::new(raw);
    if path.components().any(|component| component == Component::ParentDir) {
        return Err(policy("Desktop Commander parent path traversal is denied"));
    }
    let candidate = if path.is_absolute() {
        path.to_path_buf()
    } else {
        root.join(path)
    };
    if !candidate.starts_with(root) {
        return Err(policy(format!(
            "Desktop Commander path escapes the repository: {}",
            candidate.display()
        )));
    }
    let mut probe = candidate.as_path();
    while !probe.exists() {
        probe = probe
            .parent()
            .ok_or_else(|| policy("Desktop Commander path has no existing ancestor"))?;
    }
    let canonical_probe = probe.canonicalize()?;
    if !canonical_probe.starts_with(root) {
        return Err(policy(format!(
            "Desktop Commander path crosses a symlink outside the repository: {}",
            candidate.display()
        )));
    }
    if candidate.exists() && !candidate.canonicalize()?.starts_with(root) {
        return Err(policy(format!(
            "Desktop Commander path resolves outside the repository: {}",
            candidate.display()
        )));
    }
    Ok(candidate)
}

fn validate_process_request(tool: &str, arguments: &Value) -> MedusaResult<()> {
    if tool != "start_process" {
        return Ok(());
    }
    if arguments.get("shell").is_some() {
        return Err(policy("Desktop Commander custom shells are denied"));
    }
    let command = arguments
        .get("command")
        .and_then(Value::as_str)
        .ok_or_else(|| invalid("Desktop Commander start_process.command must be a string"))?;
    let normalized = command.trim().to_ascii_lowercase();
    if command.contains(['\n', '\r', ';', '&', '|', '`', '>', '<'])
        || command.contains("$(")
        || ["bash", "sh", "zsh", "fish", "cmd", "powershell", "pwsh"]
            .iter()
            .any(|shell| normalized == *shell || normalized.starts_with(&format!("{shell} ")))
    {
        return Err(policy(
            "Desktop Commander shell wrappers and shell operators are denied",
        ));
    }
    Ok(())
}

fn env_flag(key: &str) -> bool {
    env::var(key)
        .ok()
        .is_some_and(|value| matches!(value.trim().to_ascii_lowercase().as_str(), "1" | "true" | "yes" | "on"))
}

fn env_u64(key: &str, default: u64) -> u64 {
    env::var(key)
        .ok()
        .and_then(|value| value.parse().ok())
        .unwrap_or(default)
}

fn env_usize(key: &str, default: usize) -> usize {
    env::var(key)
        .ok()
        .and_then(|value| value.parse().ok())
        .unwrap_or(default)
}

fn csv_set(raw: &str) -> BTreeSet<String> {
    raw.split(',')
        .map(str::trim)
        .filter(|value| !value.is_empty())
        .map(str::to_owned)
        .collect()
}

fn invalid(message: impl Into<String>) -> MedusaError {
    MedusaError::new(
        ErrorCode::InvalidConfiguration,
        ErrorCategory::Validation,
        message,
    )
}

fn policy(message: impl Into<String>) -> MedusaError {
    MedusaError::new(ErrorCode::PolicyDenied, ErrorCategory::Policy, message)
}

fn execution(message: impl Into<String>) -> MedusaError {
    MedusaError::new(
        ErrorCode::ToolExecutionFailed,
        ErrorCategory::Execution,
        message,
    )
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn defaults_are_disabled_pinned_and_read_only() {
        let settings = DesktopCommanderSettings::default();
        assert!(!settings.requested());
        assert!(settings.args.iter().any(|arg| arg == PINNED_PACKAGE));
        assert!(settings.effective_tools().contains("read_file"));
        assert!(!settings.effective_tools().contains("write_file"));
        assert!(!settings.effective_tools().contains("start_process"));
    }

    #[test]
    fn path_policy_rewrites_relative_paths_and_denies_escape() {
        let directory = tempfile::tempdir().expect("tempdir");
        fs::write(directory.path().join("value.txt"), "42").expect("fixture");
        let safe = sanitize_arguments(
            directory.path(),
            &json!({"path": "value.txt", "options": {"outputPath": "result.pdf"}}),
        )
        .expect("safe arguments");
        assert!(safe["path"].as_str().expect("path").starts_with(directory.path().to_str().unwrap()));
        assert!(sanitize_arguments(directory.path(), &json!({"path": "../secret"})).is_err());
    }

    #[cfg(unix)]
    #[test]
    fn persistent_client_initializes_lists_and_calls_tools() {
        use std::os::unix::fs::PermissionsExt;

        let directory = tempfile::tempdir().expect("tempdir");
        let server = directory.path().join("fake-mcp.sh");
        fs::write(
            &server,
            r'''#!/bin/sh
while IFS= read -r line; do
  case "$line" in
    *'"method":"initialize"'*) printf '%s\n' '{"jsonrpc":"2.0","id":1,"result":{"protocolVersion":"2025-06-18","capabilities":{},"serverInfo":{"name":"fake","version":"1"}}}' ;;
    *'"method":"tools/list"'*) printf '%s\n' '{"jsonrpc":"2.0","id":2,"result":{"tools":[{"name":"read_file","description":"read","inputSchema":{"type":"object"}}]}}' ;;
    *'"method":"tools/call"'*) printf '%s\n' '{"jsonrpc":"2.0","id":3,"result":{"content":[{"type":"text","text":"fixture-ok"}]}}' ;;
  esac
done
''',
        )
        .expect("server fixture");
        let mut permissions = fs::metadata(&server).expect("metadata").permissions();
        permissions.set_mode(0o700);
        fs::set_permissions(&server, permissions).expect("permissions");
        fs::write(directory.path().join("value.txt"), "42").expect("value");

        let settings = DesktopCommanderSettings {
            enabled: true,
            command: server,
            args: Vec::new(),
            allowed_tools: BTreeSet::from(["read_file".to_owned()]),
            allow_write: false,
            allow_process: false,
            timeout: Duration::from_secs(2),
            max_output_bytes: 16 * 1024,
            configuration_error: None,
        };
        let mut client = DesktopCommanderClient::connect(directory.path(), settings)
            .expect("connect fake server");
        let result = client
            .call_tool(
                directory.path(),
                "read_file",
                &json!({"path": "value.txt"}),
                false,
            )
            .expect("call tool");
        assert_eq!(result["content"][0]["text"], "fixture-ok");
    }
}
'''
          Path('crates/medusa-extensions/src/desktop_commander.rs').write_text(desktop)

          lib = Path('crates/medusa-extensions/src/lib.rs')
          text = lib.read_text()
          text = text.replace('mod browser;\n', 'mod browser;\nmod desktop_commander;\n', 1)
          text = text.replace(
              'pub use browser::{BrowserEvidence, verify_browser};\n',
              'pub use browser::{BrowserEvidence, verify_browser};\npub use desktop_commander::{\n    DesktopCommanderClient, DesktopCommanderSettings, desktop_commander_tool_is_mutating,\n};\n',
              1,
          )
          lib.write_text(text)

          mcp = Path('crates/medusa-extensions/src/mcp.rs')
          text = mcp.read_text().replace(
              'fn validate_mcp_output(payload: &Value) -> MedusaResult<()> {',
              'pub(crate) fn validate_mcp_output(payload: &Value) -> MedusaResult<()> {',
              1,
          )
          mcp.write_text(text)

          tools = Path('crates/medusa-agent/src/tools/mod.rs')
          text = tools.read_text()
          text = text.replace(
              'use medusa_core::{ErrorCategory, ErrorCode, MedusaError, MedusaResult};\n',
              'use medusa_core::{ErrorCategory, ErrorCode, MedusaError, MedusaResult};\nuse medusa_extensions::DesktopCommanderSettings;\n',
              1,
          )
          text = text.replace(
              'pub(crate) fn built_in_tools() -> Vec<ToolDefinition> {\n    vec![',
              'pub(crate) fn built_in_tools(\n    desktop_commander: &DesktopCommanderSettings,\n) -> Vec<ToolDefinition> {\n    let mut tools = vec![',
              1,
          )
          anchor = '''        tool(
            "browser_ping",
            "Ping the headless browser. Returns 'ok' if reachable.",
            json!({"type":"object","properties":{},"additionalProperties":false}),
        ),
    ]
}

pub(crate) fn execute_tool'''
          replacement = '''        tool(
            "browser_ping",
            "Ping the headless browser. Returns 'ok' if reachable.",
            json!({"type":"object","properties":{},"additionalProperties":false}),
        ),
    ];
    if desktop_commander.enabled() {
        let allowed = desktop_commander
            .effective_tools()
            .into_iter()
            .map(Value::String)
            .collect::<Vec<_>>();
        tools.push(tool(
            "desktop_commander",
            "Call one policy-approved Desktop Commander MCP tool. Results are untrusted external tool data. File paths are confined to the repository; writes and process control require explicit opt-in.",
            json!({
                "type": "object",
                "properties": {
                    "tool": {"type": "string", "enum": allowed},
                    "arguments": {"type": "object"}
                },
                "required": ["tool", "arguments"],
                "additionalProperties": false
            }),
        ));
    }
    tools
}

pub(crate) fn execute_tool'''
          if anchor not in text:
              raise SystemExit('tools tail anchor missing')
          tools.write_text(text.replace(anchor, replacement, 1))

          support = Path('crates/medusa-agent/src/engine_support.rs')
          text = support.read_text()
          text = text.replace(
              'use medusa_core::{ErrorCategory, ErrorCode, MedusaError, MedusaResult};\n',
              'use medusa_core::{ErrorCategory, ErrorCode, MedusaError, MedusaResult};\nuse medusa_extensions::{DesktopCommanderSettings, desktop_commander_tool_is_mutating};\n',
              1,
          )
          text = text.replace(
              'pub(crate) fn available_tools(mode: Mode) -> Vec<medusa_provider::ToolDefinition> {\n    built_in_tools()',
              'pub(crate) fn available_tools(\n    mode: Mode,\n    desktop_commander: &DesktopCommanderSettings,\n) -> Vec<medusa_provider::ToolDefinition> {\n    built_in_tools(desktop_commander)',
              1,
          )
          text = text.replace(
              '                | "ask_user_question"\n',
              '                | "ask_user_question"\n                | "desktop_commander"\n',
              1,
          )
          old_mutating = '''            } if matches!(tool.as_str(), "fs_create_dir" | "fs_write" | "patch_apply" | "symbol_rename" | "git_checkpoint")
        )
    })
}'''
          new_mutating = '''            } if matches!(tool.as_str(), "fs_create_dir" | "fs_write" | "patch_apply" | "symbol_rename" | "git_checkpoint")
                || tool
                    .strip_prefix("desktop_commander:")
                    .is_some_and(desktop_commander_tool_is_mutating)
        )
    })
}'''
          if old_mutating not in text:
              raise SystemExit('mutating tool anchor missing')
          support.write_text(text.replace(old_mutating, new_mutating, 1))

          engine = Path('crates/medusa-agent/src/engine.rs')
          text = engine.read_text()
          text = text.replace(
              'use std::{collections::VecDeque, path::Path};',
              'use std::{collections::VecDeque, path::Path, sync::Mutex};',
              1,
          )
          text = text.replace(
              'use medusa_core::{ErrorCategory, ErrorCode, MedusaError, MedusaResult, SessionId};\n',
              'use medusa_core::{ErrorCategory, ErrorCode, MedusaError, MedusaResult, SessionId};\nuse medusa_extensions::{DesktopCommanderClient, DesktopCommanderSettings};\n',
              1,
          )
          text = text.replace(
              '    tools::execute_tool,',
              '    tools::{execute_tool, input_string},',
              1,
          )
          text = text.replace(
              '''pub struct AgentEngine<P> {
    provider: P,
    config: Config,
}''',
              '''pub struct AgentEngine<P> {
    provider: P,
    config: Config,
    desktop_commander_settings: DesktopCommanderSettings,
    desktop_commander: Mutex<Option<DesktopCommanderClient>>,
}''',
              1,
          )
          text = text.replace(
              '''    pub fn new(provider: P, config: Config) -> Self {
        Self { provider, config }
    }
''',
              '''    pub fn new(provider: P, config: Config) -> Self {
        Self {
            provider,
            config,
            desktop_commander_settings: DesktopCommanderSettings::from_env(),
            desktop_commander: Mutex::new(None),
        }
    }

    fn execute_desktop_commander(&self, repo: &Path, input: &serde_json::Value) -> MedusaResult<String> {
        let tool = input_string(input, "tool")?;
        let arguments = input
            .get("arguments")
            .ok_or_else(|| MedusaError::new(
                ErrorCode::InvalidConfiguration,
                ErrorCategory::Validation,
                "desktop_commander.arguments must be an object",
            ))?;
        let mut client = self
            .desktop_commander
            .lock()
            .map_err(|_| MedusaError::new(
                ErrorCode::InternalInvariant,
                ErrorCategory::Internal,
                "Desktop Commander client lock was poisoned",
            ))?;
        if client.is_none() {
            *client = Some(DesktopCommanderClient::connect(
                repo,
                self.desktop_commander_settings.clone(),
            )?);
        }
        let result = client
            .as_mut()
            .expect("Desktop Commander client initialized")
            .call_tool(
                repo,
                tool,
                arguments,
                self.config.agent.mode == Mode::ReadOnly,
            );
        if result.is_err() {
            client.take();
        }
        serde_json::to_string_pretty(&result?).map_err(Into::into)
    }
''',
              1,
          )
          text = text.replace(
              '            tools: available_tools(self.config.agent.mode),',
              '            tools: available_tools(\n                self.config.agent.mode,\n                &self.desktop_commander_settings,\n            ),',
              1,
          )
          text = text.replace(
              '''        while let Some((id, name, input)) = calls.pop_front() {
            append_observed(
                session,
                EventPayload::ToolCallRequested {
                    tool: name.clone(),''',
              '''        while let Some((id, name, input)) = calls.pop_front() {
            let event_tool = audited_tool_name(&name, &input);
            append_observed(
                session,
                EventPayload::ToolCallRequested {
                    tool: event_tool.clone(),''',
              1,
          )
          text = text.replace(
              '''            } else if tool_allowed(self.config.agent.mode, &name) {
                execute_tool(&session.repo, &name, &input)
            } else {''',
              '''            } else if name == "desktop_commander"
                && tool_allowed(self.config.agent.mode, &name)
            {
                self.execute_desktop_commander(&session.repo, &input)
            } else if tool_allowed(self.config.agent.mode, &name) {
                execute_tool(&session.repo, &name, &input)
            } else {''',
              1,
          )
          text = text.replace(
              '                        tool: name.clone(),\n                        reason: reason.clone(),',
              '                        tool: event_tool.clone(),\n                        reason: reason.clone(),',
              1,
          )
          text = text.replace(
              '''                EventPayload::ToolExecutionCompleted {
                    tool: name.clone(),''',
              '''                EventPayload::ToolExecutionCompleted {
                    tool: event_tool,''',
              1,
          )
          insert = '''
fn audited_tool_name(name: &str, input: &serde_json::Value) -> String {
    if name == "desktop_commander" {
        if let Some(tool) = input.get("tool").and_then(serde_json::Value::as_str) {
            return format!("desktop_commander:{tool}");
        }
    }
    name.to_owned()
}

'''
          marker = 'fn json_error(error: serde_json::Error) -> MedusaError {'
          if marker not in text:
              raise SystemExit('engine helper marker missing')
          engine.write_text(text.replace(marker, insert + marker, 1))

          cli = Path('crates/medusa-cli/src/main.rs')
          text = cli.read_text()
          text = text.replace(
              'use medusa_hardening::{CURRENT_SCHEMA_VERSION, Migrator};\n',
              'use medusa_extensions::DesktopCommanderSettings;\nuse medusa_hardening::{CURRENT_SCHEMA_VERSION, Migrator};\n',
              1,
          )
          text = text.replace('    let checks = vec![\n', '    let mut checks = vec![\n', 1)
          anchor = '''        },
    ];
    println!("{}", serde_json::to_string_pretty(&checks)?);'''
          replacement = '''        },
    ];
    checks.push(desktop_commander_check(&DesktopCommanderSettings::from_env()));
    println!("{}", serde_json::to_string_pretty(&checks)?);'''
          if anchor not in text:
              raise SystemExit('doctor checks anchor missing')
          text = text.replace(anchor, replacement, 1)
          helper = '''
fn desktop_commander_check(settings: &DesktopCommanderSettings) -> DoctorCheck {
    if !settings.requested() {
        return DoctorCheck {
            name: "desktop_commander_mcp",
            ok: true,
            detail: "disabled; set MEDUSA_DESKTOP_COMMANDER_ENABLED=true to opt in".to_owned(),
        };
    }
    if let Some(error) = settings.configuration_error() {
        return DoctorCheck {
            name: "desktop_commander_mcp",
            ok: false,
            detail: error.to_owned(),
        };
    }
    let available = executable_available(settings.command());
    DoctorCheck {
        name: "desktop_commander_mcp",
        ok: available,
        detail: if available {
            format!("{} via {}", settings.package_label(), settings.command().display())
        } else {
            format!("{} was not found on PATH", settings.command().display())
        },
    }
}

fn executable_available(program: &Path) -> bool {
    if program.components().count() > 1 {
        return program.is_file();
    }
    let Some(path) = std::env::var_os("PATH") else {
        return false;
    };
    std::env::split_paths(&path).any(|directory| {
        let candidate = directory.join(program);
        if candidate.is_file() {
            return true;
        }
        if cfg!(windows) {
            ["exe", "cmd", "bat"]
                .iter()
                .any(|extension| candidate.with_extension(extension).is_file())
        } else {
            false
        }
    })
}

'''
          marker = 'fn writable_directory(path: &Path) -> bool {'
          if marker not in text:
              raise SystemExit('writable directory marker missing')
          cli.write_text(text.replace(marker, helper + marker, 1))

          readme = Path('README.md')
          text = readme.read_text()
          browser = '''The agent can drive a headless browser via the `browser_*` tools (`browser_navigate`, `browser_snapshot`, `browser_click`, `browser_fill`, `browser_press`, `browser_screenshot`, `browser_evaluate`, `browser_tabs`, `browser_close`). The browser runs in a separate `medusa-browserd` sidecar process. Medusa auto-discovers it next to the agent binary or on `PATH`; set `MEDUSA_BROWSER_PATH` to override. The sidecar requires Node.js 22 and a Chromium install (the same prerequisites the verification flow uses).
'''
          section = browser + '''
### Desktop Commander MCP

Desktop Commander access is optional and disabled by default. Medusa launches a pinned `@wonderwhy-er/desktop-commander@0.2.46` stdio server through `npx`, performs the MCP initialize/list/call lifecycle, and keeps the process alive for the agent session.

```bash
export MEDUSA_DESKTOP_COMMANDER_ENABLED=true
medusa doctor
medusa
```

The integration uses a Medusa-owned isolated home under `.medusa/extensions/desktop-commander`, writes a Desktop Commander profile with telemetry and onboarding disabled, clears inherited credentials, and limits `allowedDirectories` to the active repository. Medusa independently rewrites and validates path arguments, rejects parent traversal and symlink escapes, caps/redacts MCP output, and treats all returned content as untrusted.

The default capability set is read-only: `read_file`, `read_multiple_files`, `list_directory`, `get_file_info`, and the search-session tools. Enable additional groups explicitly:

```bash
export MEDUSA_DESKTOP_COMMANDER_ALLOW_WRITE=true
export MEDUSA_DESKTOP_COMMANDER_ALLOW_PROCESS=true
```

Process control remains disabled unless explicitly enabled because Desktop Commander documents its own command blocklist and directory restrictions as advisory guardrails rather than a security sandbox. Even when enabled, Medusa denies custom shell selection, shell wrappers, shell operators, configuration mutation, telemetry/feedback, and access outside the repository.

Advanced overrides:

```bash
export MEDUSA_DESKTOP_COMMANDER_ALLOWED_TOOLS='read_file,list_directory,start_search,get_more_search_results,write_file'
export MEDUSA_DESKTOP_COMMANDER_COMMAND='npx'
export MEDUSA_DESKTOP_COMMANDER_ARGS='["-y","@wonderwhy-er/desktop-commander@0.2.46","--no-onboarding"]'
export MEDUSA_DESKTOP_COMMANDER_TIMEOUT_MS=30000
export MEDUSA_DESKTOP_COMMANDER_MAX_OUTPUT_BYTES=262144
```
'''
          if browser not in text:
              raise SystemExit('README browser anchor missing')
          readme.write_text(text.replace(browser, section, 1))
          PY
      - uses: dtolnay/rust-toolchain@1.88.0
        with:
          components: rustfmt,clippy
      - name: Format
        run: cargo fmt --all
      - name: Check source-size guardrail
        run: bash scripts/check-source-size.sh
      - name: Clippy
        run: cargo clippy --workspace --all-targets --all-features --locked -- -D warnings
      - name: Focused tests
        run: cargo test -p medusa-extensions -p medusa-agent -p medusa-cli --locked
      - name: Commit integration
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add README.md crates/medusa-extensions/src crates/medusa-agent/src crates/medusa-cli/src/main.rs
          git diff --cached --check
          git commit -m "feat(mcp): integrate Desktop Commander"
          git push origin HEAD:feat/desktop-commander-mcp
