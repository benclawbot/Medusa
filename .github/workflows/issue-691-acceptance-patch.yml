name: Issue 691 acceptance patch

on:
  push:
    branches:
      - agent/issue-691-mutation-dag

permissions:
  contents: write

jobs:
  patch:
    if: ${{ github.actor != 'github-actions[bot]' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: agent/issue-691-mutation-dag
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@39b0b3842c7e8bbf6904c0bfc3d9006fdd4dc4e0
        with:
          toolchain: 1.88.0
          components: rustfmt
      - name: Apply acceptance instrumentation and recovery patch
        shell: bash
        run: |
          set -euo pipefail
          python3 <<'PY'
          from pathlib import Path

          p = Path('crates/medusa-runtime/src/mutating_worker_coordinator.rs')
          s = p.read_text()
          old = '''#[derive(Clone, Debug)]
pub struct ParallelImplementationEvidence {
    pub dag_fingerprint: String,
    pub children: Vec<ParallelChildEvidence>,
}
'''
          new = '''#[derive(Clone, Debug)]
pub struct ParallelImplementationEvidence {
    pub dag_fingerprint: String,
    pub children: Vec<ParallelChildEvidence>,
    pub task_elapsed_ms: std::collections::BTreeMap<String, u64>,
    pub parallel_elapsed_ms: u64,
    pub wave_count: usize,
    pub peak_parallelism: usize,
}
'''
          if old not in s:
              raise SystemExit('parallel evidence anchor not found')
          s = s.replace(old, new, 1)

          old = '''    let mut completed = std::collections::BTreeSet::new();
    let mut accepted = std::collections::BTreeMap::new();
    while completed.len() < dag.tasks.len() {
'''
          new = '''    let started = std::time::Instant::now();
    let mut completed = std::collections::BTreeSet::new();
    let mut accepted = std::collections::BTreeMap::new();
    let mut task_elapsed_ms = std::collections::BTreeMap::new();
    let mut wave_count = 0usize;
    let mut peak_parallelism = 0usize;
    while completed.len() < dag.tasks.len() {
'''
          if old not in s:
              raise SystemExit('parallel loop anchor not found')
          s = s.replace(old, new, 1)

          old = '''        if wave.is_empty() {
            return Err("parallel mutation DAG has no runnable conflict-free wave".to_owned());
        }
        let results = std::thread::scope(|scope| {
'''
          new = '''        if wave.is_empty() {
            return Err("parallel mutation DAG has no runnable conflict-free wave".to_owned());
        }
        wave_count = wave_count.saturating_add(1);
        peak_parallelism = peak_parallelism.max(wave.len());
        let results = std::thread::scope(|scope| {
'''
          if old not in s:
              raise SystemExit('wave anchor not found')
          s = s.replace(old, new, 1)

          old = '''                handles.push(scope.spawn(move || {
                    let control = TeamControlPlane::default();
                    run_implementation(
                        repo,
                        config,
                        api_key,
                        &child_plan,
                        &child_preflight,
                        cancel,
                        (&control, &events),
                    )
                    .map(|evidence| (task_id, evidence))
                }));
'''
          new = '''                handles.push(scope.spawn(move || {
                    let started = std::time::Instant::now();
                    let control = TeamControlPlane::default();
                    run_implementation(
                        repo,
                        config,
                        api_key,
                        &child_plan,
                        &child_preflight,
                        cancel,
                        (&control, &events),
                    )
                    .map(|evidence| {
                        let elapsed_ms = u64::try_from(started.elapsed().as_millis()).unwrap_or(u64::MAX);
                        (task_id, evidence, elapsed_ms)
                    })
                }));
'''
          if old not in s:
              raise SystemExit('thread timing anchor not found')
          s = s.replace(old, new, 1)

          old = '''        for (task_id, evidence) in results {
            completed.insert(task_id.clone());
            accepted.insert(task_id, evidence);
        }
'''
          new = '''        for (task_id, evidence, elapsed_ms) in results {
            completed.insert(task_id.clone());
            task_elapsed_ms.insert(task_id.clone(), elapsed_ms);
            accepted.insert(task_id, evidence);
        }
'''
          if old not in s:
              raise SystemExit('result timing anchor not found')
          s = s.replace(old, new, 1)

          old = '''    Ok(ParallelImplementationEvidence {
        dag_fingerprint: dag.fingerprint.clone(),
        children,
    })
'''
          new = '''    Ok(ParallelImplementationEvidence {
        dag_fingerprint: dag.fingerprint.clone(),
        children,
        task_elapsed_ms,
        parallel_elapsed_ms: u64::try_from(started.elapsed().as_millis()).unwrap_or(u64::MAX),
        wave_count,
        peak_parallelism,
    })
'''
          if old not in s:
              raise SystemExit('parallel evidence return anchor not found')
          s = s.replace(old, new, 1)
          p.write_text(s)

          p = Path('crates/medusa-runtime/src/parallel_mutation_batch.rs')
          s = p.read_text()
          old = '''use serde::Serialize;
'''
          new = '''use serde::{Deserialize, Serialize};
'''
          if old not in s:
              raise SystemExit('serde anchor not found')
          s = s.replace(old, new, 1)

          old = '''const BATCH_WORKER_LABEL: &str = "parallel-batch";
'''
          new = '''const BATCH_WORKER_LABEL: &str = "parallel-batch";
const PARALLEL_METRICS_SCHEMA_VERSION: u16 = 1;

#[derive(Clone, Debug, Deserialize, Eq, PartialEq, Serialize)]
struct ParallelMutationMetrics {
    schema_version: u16,
    dag_fingerprint: String,
    task_count: usize,
    wave_count: usize,
    peak_parallelism: usize,
    conflict_edges: usize,
    serial_worker_ms: u64,
    parallel_worker_ms: u64,
    staging_ms: u64,
    idle_capacity_ms: u64,
    parallel_efficiency_milli: u16,
    wall_time_improvement_milli: u16,
    conflict_rate_milli: u16,
}
'''
          if old not in s:
              raise SystemExit('metrics type anchor not found')
          s = s.replace(old, new, 1)

          old = '''    let manager = WorkerManager::new(repo, batch_root.join("worktrees"))
        .map_err(|error| error.to_string())?;
'''
          new = '''    let staging_started = std::time::Instant::now();
    let manager = WorkerManager::new(repo, batch_root.join("worktrees"))
        .map_err(|error| error.to_string())?;
'''
          if old not in s:
              raise SystemExit('staging timer anchor not found')
          s = s.replace(old, new, 1)

          old = '''    };

    for task_id in &barrier.ordered_tasks {
'''
          new = '''    };
    reset_staging_for_replay(&staging.worktree, &base_head)?;

    for task_id in &barrier.ordered_tasks {
'''
          if old not in s:
              raise SystemExit('staging reset anchor not found')
          s = s.replace(old, new, 1)

          old = '''    persist_json(&prepared_evidence_path, &evidence)?;

    for child in &parallel.children {
'''
          new = '''    persist_json(&prepared_evidence_path, &evidence)?;
    let metrics = parallel_metrics(dag, &parallel, u64::try_from(staging_started.elapsed().as_millis()).unwrap_or(u64::MAX));
    persist_json(&batch_root.join("parallel-mutation-metrics.json"), &metrics)?;

    for child in &parallel.children {
'''
          if old not in s:
              raise SystemExit('metrics persistence anchor not found')
          s = s.replace(old, new, 1)

          old = '''            format!("changed_paths={:?}", evidence.changed_paths),
            "primary repository remains unchanged pending combined parent review".to_owned(),
'''
          new = '''            format!("changed_paths={:?}", evidence.changed_paths),
            format!("parallel_efficiency_milli={}", metrics.parallel_efficiency_milli),
            format!("wall_time_improvement_milli={}", metrics.wall_time_improvement_milli),
            format!("conflict_rate_milli={}", metrics.conflict_rate_milli),
            format!("idle_capacity_ms={}", metrics.idle_capacity_ms),
            format!("staging_ms={}", metrics.staging_ms),
            "primary repository remains unchanged pending combined parent review".to_owned(),
'''
          if old not in s:
              raise SystemExit('event metrics anchor not found')
          s = s.replace(old, new, 1)

          anchor = '''fn cherry_pick_without_commit(worktree: &Path, commit: &str) -> Result<(), String> {
'''
          helpers = '''fn parallel_metrics(
    dag: &MutationDag,
    parallel: &ParallelImplementationEvidence,
    staging_ms: u64,
) -> ParallelMutationMetrics {
    let serial_worker_ms = parallel.task_elapsed_ms.values().copied().sum::<u64>();
    let parallel_worker_ms = parallel.parallel_elapsed_ms.max(1);
    let capacity_ms = parallel_worker_ms.saturating_mul(parallel.peak_parallelism.max(1) as u64);
    let idle_capacity_ms = capacity_ms.saturating_sub(serial_worker_ms);
    let efficiency = if capacity_ms == 0 {
        0
    } else {
        serial_worker_ms.saturating_mul(1_000).saturating_div(capacity_ms).min(1_000)
    };
    let improvement = if serial_worker_ms == 0 {
        0
    } else {
        serial_worker_ms
            .saturating_sub(parallel_worker_ms)
            .saturating_mul(1_000)
            .saturating_div(serial_worker_ms)
            .min(1_000)
    };
    let pairs = dag.tasks.len().saturating_mul(dag.tasks.len().saturating_sub(1)) / 2;
    let conflict_rate = if pairs == 0 {
        0
    } else {
        dag.conflict_edges
            .len()
            .saturating_mul(1_000)
            .saturating_div(pairs)
            .min(1_000)
    };
    ParallelMutationMetrics {
        schema_version: PARALLEL_METRICS_SCHEMA_VERSION,
        dag_fingerprint: dag.fingerprint.clone(),
        task_count: dag.tasks.len(),
        wave_count: parallel.wave_count,
        peak_parallelism: parallel.peak_parallelism,
        conflict_edges: dag.conflict_edges.len(),
        serial_worker_ms,
        parallel_worker_ms,
        staging_ms,
        idle_capacity_ms,
        parallel_efficiency_milli: u16::try_from(efficiency).unwrap_or(1_000),
        wall_time_improvement_milli: u16::try_from(improvement).unwrap_or(1_000),
        conflict_rate_milli: u16::try_from(conflict_rate).unwrap_or(1_000),
    }
}

fn reset_staging_for_replay(worktree: &Path, base_head: &str) -> Result<(), String> {
    let _ = Command::new("git")
        .args(["cherry-pick", "--abort"])
        .current_dir(worktree)
        .output();
    for args in [
        vec!["reset", "--hard", base_head],
        vec!["clean", "-fd"],
    ] {
        let output = Command::new("git")
            .args(&args)
            .current_dir(worktree)
            .output()
            .map_err(|error| error.to_string())?;
        if !output.status.success() {
            return Err(format!(
                "could not reset parallel staging for deterministic replay: {}",
                String::from_utf8_lossy(&output.stderr).trim()
            ));
        }
    }
    Ok(())
}

'''
          if anchor not in s:
              raise SystemExit('helper insertion anchor not found')
          s = s.replace(anchor, helpers + anchor, 1)

          tests = r'''

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn metrics_report_parallel_gain_efficiency_idle_and_conflicts() {
        let parallel = ParallelImplementationEvidence {
            dag_fingerprint: "dag".to_owned(),
            children: Vec::new(),
            task_elapsed_ms: [("a".to_owned(), 100), ("b".to_owned(), 100)]
                .into_iter()
                .collect(),
            parallel_elapsed_ms: 110,
            wave_count: 1,
            peak_parallelism: 2,
        };
        let dag = MutationDag {
            schema_version: 1,
            repository_revision: "base".to_owned(),
            tasks: Vec::new(),
            conflict_edges: Vec::new(),
            max_parallelism: 2,
            fingerprint: "dag".to_owned(),
        };
        let metrics = parallel_metrics(&dag, &parallel, 7);
        assert_eq!(metrics.serial_worker_ms, 200);
        assert_eq!(metrics.parallel_worker_ms, 110);
        assert_eq!(metrics.wall_time_improvement_milli, 450);
        assert_eq!(metrics.parallel_efficiency_milli, 909);
        assert_eq!(metrics.idle_capacity_ms, 20);
        assert_eq!(metrics.staging_ms, 7);
    }
}
'''
          s += tests
          p.write_text(s)
          PY
          cargo fmt --all
          git rm .github/workflows/issue-691-acceptance-patch.yml
          git add crates/medusa-runtime/src/mutating_worker_coordinator.rs crates/medusa-runtime/src/parallel_mutation_batch.rs
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'feat: record parallel mutation efficiency and recover staging'
          git push origin HEAD:agent/issue-691-mutation-dag
