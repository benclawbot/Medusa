# Rustfmt debt

The repository currently contains a pre-existing formatting patch spanning recently added browser, output-envelope, configuration-test, and TUI-test files. This blocks the main workspace-quality job before clippy and tests run.

The authoritative patch is generated by `cargo fmt --all -- --check` in CI. Resolve it as a standalone formatting-only change before behavioral modularization work, then delete this note.

Tracked affected areas:

- `crates/medusa-agent/src/engine.rs`
- `crates/medusa-agent/src/output_envelope.rs`
- `crates/medusa-agent/src/session_browser.rs`
- `crates/medusa-agent/src/tools/browser.rs`
- `crates/medusa-agent/src/tools/browser_dispatch.rs`
- `crates/medusa-agent/tests/envelope_coverage.rs`
- `crates/medusa-browser-client/`
- `crates/medusa-browserd/`
- recent configuration and TUI coverage tests

No source-size exception may be raised to work around formatting or modularization failures.
