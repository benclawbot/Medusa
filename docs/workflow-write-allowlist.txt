# Workflow files that require `contents: write`.
# Format: repository-relative workflow path|specific justification
#
# Keep this registry empty unless a retained workflow has a reviewed,
# non-self-modifying reason to write repository contents. Direct `git push`
# and workflow self-deletion are forbidden even for registered entries.
.github/workflows/bootstrap-desktop-release.yml|Creates the one-time Medusa Desktop 1.0.0 draft GitHub Release with independently rebuilt, validated, checksummed, and attested Linux, macOS, and Windows assets; it never pushes commits or publishes the release.
.github/workflows/publish-release.yml|Creates a draft GitHub Release and uploads validated assets for an existing version tag; it never pushes commits or publishes the release.
.github/workflows/refresh-cli-release-assets.yml|Replaces only the three CLI archives on an already-created draft release with validated dual-binary packages built from the immutable tagged commit; it never pushes commits or publishes the release.
.github/workflows/sign-draft-release.yml|Replaces assets only on an existing immutable draft release after protected, platform-native signing and verification complete for Linux, macOS, and Windows; it never pushes commits or publishes the release.
